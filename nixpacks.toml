# Nixpacks detectará automaticamente Node.js 22 através do engines.node no package.json
# ou através da variável de ambiente NIXPACKS_NODE_VERSION=22

[phases.install]
cmds = ["npm ci --omit=dev"]

[phases.build]
cmds = ["mkdir -p server/uploads"]

[start]
cmd = "npx ts-node server/src/server.ts"
